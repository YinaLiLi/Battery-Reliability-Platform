[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers
markers =
    unit: tests without running external services
    spark: local JVM/Spark tests
    survival: pinned Survival runtime tests
    airflow: Airflow DAG tests
    integration: Docker service integration
